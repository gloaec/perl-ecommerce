{
  database => {
    type => 'mysql',
    host => 'localhost',
    name => 'perlecommerce',
    user => 'root',
    password => 'turtoise',
#   port => '5432',
  },

# database_prefix => 'perlecommerce',
  database_tables => {
    products => 'products',
  },

  used_quotas => 0,

  new_quota_table => 0
}
